# autogenerated by ssc-gen. DO NOT EDIT
"""
universal parser for kodik.info/serial entrypoint
"""

import json
import re
import sys
from typing import TypedDict, List, Dict, Union
from html import unescape as _html_unescape
from lxml import html
from lxml.html import HtmlElement

FALLBACK_HTML_STR = "<html><body></body></html>"
_RE_HEX_ENTITY = re.compile(r"&#x([0-9a-fA-F]+);")
_RE_UNICODE_ENTITY = re.compile(r"\\u([0-9a-fA-F]{4})")
_RE_BYTES_ENTITY = re.compile(r"\\x([0-9a-fA-F]{2})")
_RE_CHARS_MAP = {"\\b": "\\b", "\\f": "\\f", "\\n": "\\n", "\\r": "\\r", "\\t": "\\t"}


def repl_map(s: str, rmap: Dict[str, str]) -> str:
    for k, v in rmap.items():
        s = s.replace(k, v)
    return s


def normalize_text(text: str) -> str:
    return " ".join(text.split()) if text else ""


class _UnmatchedTableRow:
    pass


def unescape_text(text: str) -> str:
    s = _html_unescape(text)
    s = _RE_HEX_ENTITY.sub(lambda m: chr(int(m.group(1), 16)), s)
    s = _RE_UNICODE_ENTITY.sub(lambda m: chr(int(m.group(1), 16)), s)
    s = _RE_BYTES_ENTITY.sub(lambda m: chr(int(m.group(1), 16)), s)
    for ch, r in _RE_CHARS_MAP.items():
        s = s.replace(ch, r)
    return s


if sys.version_info >= (3, 9):

    def rm_prefix(s: str, p: str) -> str:
        return s.removeprefix(p)

    def rm_suffix(s: str, p: str) -> str:
        return s.removesuffix(p)


else:

    def rm_prefix(s: str, p: str) -> str:
        return s[len(p) :] if s.startswith(p) else s

    def rm_suffix(s: str, p: str) -> str:
        return s[: -(len(p))] if s.endswith(p) else s


UNMATCHED_TABLE_ROW = _UnmatchedTableRow()


UrlParamsJson = TypedDict(
    "UrlParamsJson",
    {
        "d": str,
        "d_sign": str,
        "pd": str,
        "pd_sign": str,
        "ref": str,
        "ref_sign": str,
        "advert_debug": bool,
        "min_age": int,
        "first_url": bool,
    },
)


class KodikAPIPayloadType(TypedDict):
    d: str
    d_sign: str
    pd: str
    pd_sign: str
    ref: str
    ref_sign: str
    type: str
    hash: str
    id: str


class PageMainKodikMinType(TypedDict):
    url_params: UrlParamsJson
    api_payload: KodikAPIPayloadType
    player_js_path: str


class SeasonBoxType(TypedDict):
    value: str
    data_serial_id: str
    data_serial_hash: str
    data_title: str
    data_translation_title: str


class SeriesBoxType(TypedDict):
    value: str
    data_hash: str
    data_title: str


class SeriesOptionItemType(TypedDict):
    value: str
    data_id: str
    data_hash: str
    data_title: str


SeriesOptionsType = Dict[str, List[SeriesOptionItemType]]


class TranslationsBoxType(TypedDict):
    value: str
    data_id: str
    data_translation_type: str
    data_media_id: str
    data_media_hash: str
    data_media_type: str
    data_title: str
    data_episode_count: str


class PageMainKodikSerialType(TypedDict):
    url_params: UrlParamsJson
    api_payload: KodikAPIPayloadType
    player_js_path: str
    thumbnails: List[str]
    season_box: List[SeasonBoxType]
    series_box: List[SeriesBoxType]
    series_options: SeriesOptionsType
    translations_box: List[TranslationsBoxType]


class MovieTranslationBoxType(TypedDict):
    value: str
    data_id: str
    data_translation_type: str
    data_media_id: str
    data_media_hash: str
    data_media_type: str
    data_title: str


class PageMainKodikVideoType(TypedDict):
    url_params: UrlParamsJson
    api_payload: KodikAPIPayloadType
    player_js_path: str
    api_path: str


class KodikAPIPayload:
    """
    payload for Kodik API request
    """

    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, HtmlElement):
            self._doc = document
        elif isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)
        self._raw_html = self._init_raw_html(self._doc)

    def _init_raw_html(self, v: HtmlElement) -> str:
        v1 = html.tostring(v, encoding="unicode")
        return v1

    def _parse_d(self, v: HtmlElement) -> str:
        v1 = self._raw_html
        v2 = re.search("var\\s*domain\\s+=\\s+['\\\"](.*?)['\\\"];", v1)[1]
        return v2

    def _parse_d_sign(self, v: HtmlElement) -> str:
        v1 = self._raw_html
        v2 = re.search("var\\s*d_sign\\s+=\\s+['\\\"](.*?)['\\\"];", v1)[1]
        return v2

    def _parse_pd(self, v: HtmlElement) -> str:
        v1 = self._raw_html
        v2 = re.search("var\\s*pd\\s+=\\s+['\\\"](.*?)['\\\"];", v1)[1]
        return v2

    def _parse_pd_sign(self, v: HtmlElement) -> str:
        v1 = self._raw_html
        v2 = re.search("var\\s*pd_sign\\s+=\\s+['\\\"](.*?)['\\\"];", v1)[1]
        return v2

    def _parse_ref(self, v: HtmlElement) -> str:
        v1 = self._raw_html
        v2 = re.search("var\\s*ref\\s+=\\s+['\\\"](.*?)['\\\"];", v1)[1]
        return v2

    def _parse_ref_sign(self, v: HtmlElement) -> str:
        v1 = self._raw_html
        v2 = re.search("var\\s*ref_sign\\s+=\\s+['\\\"](.*?)['\\\"];", v1)[1]
        return v2

    def _parse_type(self, v: HtmlElement) -> str:
        v1 = self._raw_html
        v2 = re.search("vInfo\\.type = ['\\\"](.*?)['\\\"];", v1)[1]
        return v2

    def _parse_hash(self, v: HtmlElement) -> str:
        v1 = self._raw_html
        v2 = re.search("vInfo\\.hash = ['\\\"](.*?)['\\\"];", v1)[1]
        return v2

    def _parse_id(self, v: HtmlElement) -> str:
        v1 = self._raw_html
        v2 = re.search("vInfo\\.id = ['\\\"](.*?)['\\\"];", v1)[1]
        return v2

    def parse(self) -> KodikAPIPayloadType:
        return {
            "d": self._parse_d(self._doc),
            "d_sign": self._parse_d_sign(self._doc),
            "pd": self._parse_pd(self._doc),
            "pd_sign": self._parse_pd_sign(self._doc),
            "ref": self._parse_ref(self._doc),
            "ref_sign": self._parse_ref_sign(self._doc),
            "type": self._parse_type(self._doc),
            "hash": self._parse_hash(self._doc),
            "id": self._parse_id(self._doc),
        }


class PageMainKodikMin:
    """

    Universal parser for extract minimal data for next API reuqest

    For extract full detailed information (about attached playlists) use:

    - MainKodikSerialPage for serials, OVA
    - MainKodikVideoPage for films

    USAGE:

        1. GET <kodik-page-player>
        2. add base_url to <player_js_path>
        3. extract API (<API_PATH>) path from javascript file (use MainKodikAPIPath)
        3.1 decode <API_PATH> path (base64 cipher)
        4. <api_payload> required extra constant keys
            - bad_user = (true or false)
            - cdn_is_working = true
            - info "{}"
        4.1 required headers:
            - origin="https://<NETLOC>" // player page
            - referer=<PLAYER_LINK> // FIRST URL player entrypoint
            - accept= "application/json, text/javascript, */*; q=0.01"
        4.2 POST <kodik-base-url> + /<API_PATH>
           data=<api_payload> (<JSON>) + headers
        5. extract urls from ['links'] key
        6. video urls encoded in ROT_13 + BASE64 ciphers

    EXAMPLE:

        - GET https://kodik.info/serial/64218/890744b309ec026d43742995d0d49cd7/720p?season=1&episode=1
        - GET https://aniqit.com/video/72755/dc966c03a7cb719dac577d8004a9b091/720p
        - GET https://kodik.info/seria/1133512/04d5f7824ba3563bd78e44a22451bb45/720p

     ISSUES:

        - kodik maybe have another netloc (e.g.: anivod)
        - 403 Forbidden if request sent not from CIS region
        - 404 DELETED: eg: https://kodik.info/seria/310427/09985563d891b56b1e9b01142ae11872/720p
        - 500 Internal server error: eg: https://kodik.info/seria/1051016/af405efc5e061f5ac344d4811de3bc16/720p ('Cyberpunk: Edgerunners' ep5 Anilibria dub)


    """

    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, HtmlElement):
            self._doc = document
        elif isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)

    def _parse_url_params(self, v: HtmlElement) -> UrlParamsJson:
        v1 = html.tostring(v, encoding="unicode")
        v2 = re.search("var\\s*urlParams\\s*=\\s*['\\\"](\\{.*\\})['\\\"]", v1)[1]
        v3 = json.loads(v2)
        return v3

    def _parse_api_payload(self, v: HtmlElement) -> KodikAPIPayloadType:
        v1 = KodikAPIPayload(v).parse()
        return v1

    def _parse_player_js_path(self, v: HtmlElement) -> str:
        v1 = v.cssselect('head > script[type="text/javascript"][src*="assets/js"]')[0]
        v2 = v1.get("src", "")
        return v2

    def parse(self) -> PageMainKodikMinType:
        return {
            "url_params": self._parse_url_params(self._doc),
            "api_payload": self._parse_api_payload(self._doc),
            "player_js_path": self._parse_player_js_path(self._doc),
        }


class SeasonBox:
    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, HtmlElement):
            self._doc = document
        elif isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)

    def _split_doc(self, v: HtmlElement) -> List[HtmlElement]:
        v1 = v.cssselect(".serial-panel > .serial-seasons-box option")
        return v1

    def _parse_value(self, v: HtmlElement) -> str:
        v1 = v.get("value", "")
        return v1

    def _parse_data_serial_id(self, v: HtmlElement) -> str:
        v1 = v.get("data-serial-id", "")
        return v1

    def _parse_data_serial_hash(self, v: HtmlElement) -> str:
        v1 = v.get("data-serial-hash", "")
        return v1

    def _parse_data_title(self, v: HtmlElement) -> str:
        v1 = v.get("data-title", "")
        return v1

    def _parse_data_translation_title(self, v: HtmlElement) -> str:
        v1 = v.get("data-translation-title", "")
        return v1

    def parse(self) -> List[SeasonBoxType]:
        return [
            {
                "value": self._parse_value(i),
                "data_serial_id": self._parse_data_serial_id(i),
                "data_serial_hash": self._parse_data_serial_hash(i),
                "data_title": self._parse_data_title(i),
                "data_translation_title": self._parse_data_translation_title(i),
            }
            for i in self._split_doc(self._doc)
        ]


class SeriesBox:
    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, HtmlElement):
            self._doc = document
        elif isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)

    def _split_doc(self, v: HtmlElement) -> List[HtmlElement]:
        v1 = v.cssselect(".serial-panel > .serial-series-box option")
        return v1

    def _parse_value(self, v: HtmlElement) -> str:
        v1 = v.get("value", "")
        return v1

    def _parse_data_hash(self, v: HtmlElement) -> str:
        v1 = v.get("data-hash", "")
        return v1

    def _parse_data_title(self, v: HtmlElement) -> str:
        v1 = v.get("data-title", "")
        return v1

    def parse(self) -> List[SeriesBoxType]:
        return [
            {
                "value": self._parse_value(i),
                "data_hash": self._parse_data_hash(i),
                "data_title": self._parse_data_title(i),
            }
            for i in self._split_doc(self._doc)
        ]


class SeriesOptionItem:
    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, HtmlElement):
            self._doc = document
        elif isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)

    def _split_doc(self, v: HtmlElement) -> List[HtmlElement]:
        v1 = v.cssselect("option")
        return v1

    def _parse_value(self, v: HtmlElement) -> str:
        v1 = v.get("value", "")
        return v1

    def _parse_data_id(self, v: HtmlElement) -> str:
        v1 = v.get("data-id", "")
        return v1

    def _parse_data_hash(self, v: HtmlElement) -> str:
        v1 = v.get("data-hash", "")
        return v1

    def _parse_data_title(self, v: HtmlElement) -> str:
        v1 = v.get("data-title", "")
        return v1

    def parse(self) -> List[SeriesOptionItemType]:
        return [
            {
                "value": self._parse_value(i),
                "data_id": self._parse_data_id(i),
                "data_hash": self._parse_data_hash(i),
                "data_title": self._parse_data_title(i),
            }
            for i in self._split_doc(self._doc)
        ]


class SeriesOptions:
    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, HtmlElement):
            self._doc = document
        elif isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)

    def _split_doc(self, v: HtmlElement) -> List[HtmlElement]:
        v1 = v.cssselect(".serial-panel > .series-options div")
        return v1

    def _parse_key(self, v: HtmlElement) -> str:
        v1 = v.get("class", "")
        return v1

    def _parse_value(self, v: HtmlElement) -> List[SeriesOptionItemType]:
        v1 = SeriesOptionItem(v).parse()
        return v1

    def parse(self) -> SeriesOptionsType:
        return {self._parse_key(i): self._parse_value(i) for i in self._split_doc(self._doc)}


class TranslationsBox:
    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, HtmlElement):
            self._doc = document
        elif isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)

    def _split_doc(self, v: HtmlElement) -> List[HtmlElement]:
        v1 = v.cssselect(".serial-panel > .serial-translations-box option")
        return v1

    def _parse_value(self, v: HtmlElement) -> str:
        v1 = v.get("value", "")
        return v1

    def _parse_data_id(self, v: HtmlElement) -> str:
        v1 = v.get("data-id", "")
        return v1

    def _parse_data_translation_type(self, v: HtmlElement) -> str:
        v1 = v.get("data-translation-type", "")
        return v1

    def _parse_data_media_id(self, v: HtmlElement) -> str:
        v1 = v.get("data-media-id", "")
        return v1

    def _parse_data_media_hash(self, v: HtmlElement) -> str:
        v1 = v.get("data-media-hash", "")
        return v1

    def _parse_data_media_type(self, v: HtmlElement) -> str:
        v1 = v.get("data-media-type", "")
        return v1

    def _parse_data_title(self, v: HtmlElement) -> str:
        v1 = v.get("data-title", "")
        return v1

    def _parse_data_episode_count(self, v: HtmlElement) -> str:
        v1 = v.get("data-episode-count", "")
        v2 = re.search("(\\d+)$", v1)[1]
        return v2

    def parse(self) -> List[TranslationsBoxType]:
        return [
            {
                "value": self._parse_value(i),
                "data_id": self._parse_data_id(i),
                "data_translation_type": self._parse_data_translation_type(i),
                "data_media_id": self._parse_data_media_id(i),
                "data_media_hash": self._parse_data_media_hash(i),
                "data_media_type": self._parse_data_media_type(i),
                "data_title": self._parse_data_title(i),
                "data_episode_count": self._parse_data_episode_count(i),
            }
            for i in self._split_doc(self._doc)
        ]


class PageMainKodikSerial:
    """

    First extract data entrypoint for kodik.../serial/ entrypoint path


    EXAMPLE:
        GET - https://kodik.info/serial/64218/890744b309ec026d43742995d0d49cd7/720p?season=1&episode=1

    """

    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, HtmlElement):
            self._doc = document
        elif isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)

    def _parse_url_params(self, v: HtmlElement) -> UrlParamsJson:
        v1 = html.tostring(v, encoding="unicode")
        v2 = re.search("var\\s*urlParams\\s*=\\s*['\\\"](\\{.*\\})['\\\"]", v1)[1]
        v3 = json.loads(v2)
        return v3

    def _parse_api_payload(self, v: HtmlElement) -> KodikAPIPayloadType:
        v1 = KodikAPIPayload(v).parse()
        return v1

    def _parse_player_js_path(self, v: HtmlElement) -> str:
        v1 = v.cssselect('head > script[type="text/javascript"][src*="assets/js"]')[0]
        v2 = v1.get("src", "")
        return v2

    def _parse_thumbnails(self, v: HtmlElement) -> List[str]:
        v1 = html.tostring(v, encoding="unicode")
        v2 = re.search("var\\s*thumbnails\\s*=\\s*\\[(.*?)\\];", v1)[1]
        v3 = v2.replace('"', "")
        v4 = v3.split(",")
        v5 = [i.strip() for i in v4]
        v6 = ["https:{}".format(i) for i in v5]
        return v6

    def _parse_season_box(self, v: HtmlElement) -> List[SeasonBoxType]:
        v1 = SeasonBox(v).parse()
        return v1

    def _parse_series_box(self, v: HtmlElement) -> List[SeriesBoxType]:
        v1 = SeriesBox(v).parse()
        return v1

    def _parse_series_options(self, v: HtmlElement) -> SeriesOptionsType:
        v1 = SeriesOptions(v).parse()
        return v1

    def _parse_translations_box(self, v: HtmlElement) -> List[TranslationsBoxType]:
        v1 = TranslationsBox(v).parse()
        return v1

    def parse(self) -> PageMainKodikSerialType:
        return {
            "url_params": self._parse_url_params(self._doc),
            "api_payload": self._parse_api_payload(self._doc),
            "player_js_path": self._parse_player_js_path(self._doc),
            "thumbnails": self._parse_thumbnails(self._doc),
            "season_box": self._parse_season_box(self._doc),
            "series_box": self._parse_series_box(self._doc),
            "series_options": self._parse_series_options(self._doc),
            "translations_box": self._parse_translations_box(self._doc),
        }


class MovieTranslationBox:
    """
    PageMainKodikVideo /video/ (film) segment
    """

    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, HtmlElement):
            self._doc = document
        elif isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)

    def _split_doc(self, v: HtmlElement) -> List[HtmlElement]:
        v1 = v.cssselect(".movie-panel > .movie-translations-box option")
        return v1

    def _parse_value(self, v: HtmlElement) -> str:
        v1 = v.get("value", "")
        return v1

    def _parse_data_id(self, v: HtmlElement) -> str:
        v1 = v.get("data-id", "")
        return v1

    def _parse_data_translation_type(self, v: HtmlElement) -> str:
        v1 = v.get("data-translation-type", "")
        return v1

    def _parse_data_media_id(self, v: HtmlElement) -> str:
        v1 = v.get("data-media-id", "")
        return v1

    def _parse_data_media_hash(self, v: HtmlElement) -> str:
        v1 = v.get("data-media-hash", "")
        return v1

    def _parse_data_media_type(self, v: HtmlElement) -> str:
        v1 = v.get("data-media-type", "")
        return v1

    def _parse_data_title(self, v: HtmlElement) -> str:
        v1 = v.get("data-title", "")
        return v1

    def parse(self) -> List[MovieTranslationBoxType]:
        return [
            {
                "value": self._parse_value(i),
                "data_id": self._parse_data_id(i),
                "data_translation_type": self._parse_data_translation_type(i),
                "data_media_id": self._parse_data_media_id(i),
                "data_media_hash": self._parse_data_media_hash(i),
                "data_media_type": self._parse_data_media_type(i),
                "data_title": self._parse_data_title(i),
            }
            for i in self._split_doc(self._doc)
        ]


class PageMainKodikVideo:
    """

    First extract data entrypoint for kodik.../video/ entrypoint path (FILMS, less often OVA)

    required for extract videos via kodik API

    EXAMPLE:

        - GET https://kodik.info/video/9935/313bc89421b094f6f374cc7420e00ad1/720p?translations=false&min_age=16 (Кланнад фильм)
        - GET https://kodik.info/video/72755/dc966c03a7cb719dac577d8004a9b091/720p?translations=false&min_age=16 (Вайолет Эвергарден. Фильм)

    """

    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, HtmlElement):
            self._doc = document
        elif isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)

    def _parse_url_params(self, v: HtmlElement) -> UrlParamsJson:
        v1 = html.tostring(v, encoding="unicode")
        v2 = re.search("var\\s*urlParams\\s*=\\s*['\\\"](\\{.*\\})['\\\"]", v1)[1]
        v3 = json.loads(v2)
        return v3

    def _parse_api_payload(self, v: HtmlElement) -> KodikAPIPayloadType:
        v1 = KodikAPIPayload(v).parse()
        return v1

    def _parse_player_js_path(self, v: HtmlElement) -> str:
        v1 = v.cssselect('head > script[type="text/javascript"][src*="assets/js"]')[0]
        v2 = v1.get("src", "")
        return v2

    def _parse_api_path(self, v: HtmlElement) -> str:
        v1 = html.tostring(v, encoding="unicode")
        v2 = re.search("\\$\\.ajax[^)]+atob\\([\\\"\\'](\\w+=)[\\'\\\"]\\)", v1)[1]
        return v2

    def parse(self) -> PageMainKodikVideoType:
        return {
            "url_params": self._parse_url_params(self._doc),
            "api_payload": self._parse_api_payload(self._doc),
            "player_js_path": self._parse_player_js_path(self._doc),
            "api_path": self._parse_api_path(self._doc),
        }
