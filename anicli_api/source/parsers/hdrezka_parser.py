# autogenerated by ssc-gen. DO NOT EDIT
from lxml import html
from lxml.html import HtmlElement
from typing import Any, Dict, List, TypedDict, Union
from dataclasses import dataclass
from typing import Literal
from typing import cast
import httpx
from .sscgen_runtime import (
    FALLBACK_HTML_STR,
    Ok,
    Err,
    UnknownErr,
    TransportErr,
    ErrMatcher,
    ssc_rest_call,
    ssc_rest_call_async,
)

from .sscgen_runtime import std_re_search

HdrezkaCdnResponseJson = TypedDict(
    "HdrezkaCdnResponseJson",
    {
        "success": bool,
        "message": str,
        "premium_content": int,
        "url": str,
        "quality": str,
        "subtitle": bool,
        "subtitle_lns": bool,
        "subtitle_def": bool,
        "thumbnails": str,
    },
)
HdrezkaCdnErrorResponseJson = TypedDict(
    "HdrezkaCdnErrorResponseJson",
    {
        "success": bool,
        "message": str,
    },
)
PageOngoingType = TypedDict(
    "PageOngoingType",
    {
        "title": str,
        "season": str,
        "thumbnail": str,
        "url": str,
    },
)
PageSearchType = TypedDict(
    "PageSearchType",
    {
        "title": str,
        "season": str,
        "thumbnail": str,
        "url": str,
    },
)
TranslationType = TypedDict(
    "TranslationType",
    {
        "title": str,
        "data_translator_id": str,
    },
)
SeasonBoxType = TypedDict(
    "SeasonBoxType",
    {
        "data_tab_id": str,
        "title": str,
    },
)
EpisodeType = TypedDict(
    "EpisodeType",
    {
        "data_id": int,
        "data_season_id": int,
        "data_episode_id": int,
        "title": str,
    },
)
PageAnimeType = TypedDict(
    "PageAnimeType",
    {
        "title": str,
        "description": str,
        "thumbnail": str,
        "favs": str,
        "translation_list": List[TranslationType],
        "season_box": List[SeasonBoxType],
        "episode_list": List[EpisodeType],
        "id": int,
        "translation_id": str,
    },
)


class PageOngoing:
    """
    Get all available ongoings from the main page

    USAGE
        GET https://hdrezka-home.tv/?filter=last&genre=82
    """

    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)
        else:
            self._doc = document

    def _split_doc(self, v: HtmlElement) -> List[HtmlElement]:
        v1 = v.cssselect(".b-content__inline_item")
        return v1

    @classmethod
    def fetch(cls, client: httpx.Client, **kwargs: Any) -> "PageOngoing":
        _kw: Dict[str, Any] = {}
        _kw["params"] = {"filter": "last", "genre": "82"}
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        _resp = client.request(
            "GET",
            "https://hdrezka-home.tv/",
            **_kw,
        )
        _resp.raise_for_status()
        _body = _resp.text
        return cls(_body)

    @classmethod
    async def async_fetch(cls, client: httpx.AsyncClient, **kwargs: Any) -> "PageOngoing":
        _kw: Dict[str, Any] = {}
        _kw["params"] = {"filter": "last", "genre": "82"}
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        _resp = await client.request(
            "GET",
            "https://hdrezka-home.tv/",
            **_kw,
        )
        _resp.raise_for_status()
        _body = _resp.text
        return cls(_body)

    def _parse_title(self, v: HtmlElement) -> str:
        v1 = v.cssselect(".b-content__inline_item-link a")[0]
        v2 = v1.text_content()
        v3 = v2.strip()
        return v3

    def _parse_season(self, v: HtmlElement) -> str:
        try:
            v1 = v.cssselect("span.info")[0]
            v2 = v1.text_content()
            return v2
        except Exception:
            return ""

    def _parse_thumbnail(self, v: HtmlElement) -> str:
        v1 = v.cssselect(".b-content__inline_item img[src]")[0]
        v2 = v1.get("src", "")
        return v2

    def _parse_url(self, v: HtmlElement) -> str:
        v1 = v.cssselect("a[href]")[0]
        v2 = v1.get("href", "")
        return v2

    def parse(self) -> List[PageOngoingType]:
        return [
            {
                "title": self._parse_title(i),
                "season": self._parse_season(i),
                "thumbnail": self._parse_thumbnail(i),
                "url": self._parse_url(i),
            }
            for i in self._split_doc(self._doc)
        ]


class PageSearch:
    """
    USAGE
        GET https://hdrezka-home.tv/search/
        do=search&subaction=search&q={query}

    EXAMPLE
    https://hdrezka-home.tv/search/?do=search&subaction=search&q=isekai
    """

    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)
        else:
            self._doc = document

    def _split_doc(self, v: HtmlElement) -> List[HtmlElement]:
        v1 = v.cssselect(".b-content__inline_item")
        v2 = [i for i in v1 if "data-url" in i.attrib and "/animation/" in i.get("data-url", "")]
        return v2

    @classmethod
    def fetch(cls, client: httpx.Client, *, query: str, **kwargs: Any) -> "PageSearch":
        _kw: Dict[str, Any] = {}
        _kw["params"] = {"do": "search", "subaction": "search", "q": query}
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        _resp = client.request(
            "GET",
            "https://hdrezka-home.tv/search/",
            **_kw,
        )
        _resp.raise_for_status()
        _body = _resp.text
        return cls(_body)

    @classmethod
    async def async_fetch(cls, client: httpx.AsyncClient, *, query: str, **kwargs: Any) -> "PageSearch":
        _kw: Dict[str, Any] = {}
        _kw["params"] = {"do": "search", "subaction": "search", "q": query}
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        _resp = await client.request(
            "GET",
            "https://hdrezka-home.tv/search/",
            **_kw,
        )
        _resp.raise_for_status()
        _body = _resp.text
        return cls(_body)

    def _parse_title(self, v: HtmlElement) -> str:
        v1 = v.cssselect(".b-content__inline_item-link a")[0]
        v2 = v1.text_content()
        v3 = v2.strip()
        return v3

    def _parse_season(self, v: HtmlElement) -> str:
        try:
            v1 = v.cssselect("span.info")[0]
            v2 = v1.text_content()
            return v2
        except Exception:
            return ""

    def _parse_thumbnail(self, v: HtmlElement) -> str:
        v1 = v.cssselect(".b-content__inline_item img[src]")[0]
        v2 = v1.get("src", "")
        return v2

    def _parse_url(self, v: HtmlElement) -> str:
        v1 = v.cssselect("a[href]")[0]
        v2 = v1.get("href", "")
        return v2

    def parse(self) -> List[PageSearchType]:
        return [
            {
                "title": self._parse_title(i),
                "season": self._parse_season(i),
                "thumbnail": self._parse_thumbnail(i),
                "url": self._parse_url(i),
            }
            for i in self._split_doc(self._doc)
        ]


class Translation:
    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)
        else:
            self._doc = document

    def _split_doc(self, v: HtmlElement) -> List[HtmlElement]:
        v1 = v.cssselect("#translators-list > li.b-translator__item")
        return v1

    def _parse_title(self, v: HtmlElement) -> str:
        v1 = v.get("title", "")
        return v1

    def _parse_data_translator_id(self, v: HtmlElement) -> str:
        v1 = v.get("data-translator_id", "")
        return v1

    def parse(self) -> List[TranslationType]:
        return [
            {
                "title": self._parse_title(i),
                "data_translator_id": self._parse_data_translator_id(i),
            }
            for i in self._split_doc(self._doc)
        ]


class SeasonBox:
    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)
        else:
            self._doc = document

    def _split_doc(self, v: HtmlElement) -> List[HtmlElement]:
        v1 = v.cssselect("#simple-seasons-tabs > .b-simple_season__item")
        return v1

    def _parse_data_tab_id(self, v: HtmlElement) -> str:
        v1 = v.get("data-tab_id", "")
        return v1

    def _parse_title(self, v: HtmlElement) -> str:
        v1 = v.text_content()
        v2 = v1.strip()
        return v2

    def parse(self) -> List[SeasonBoxType]:
        return [
            {
                "data_tab_id": self._parse_data_tab_id(i),
                "title": self._parse_title(i),
            }
            for i in self._split_doc(self._doc)
        ]


class Episode:
    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)
        else:
            self._doc = document

    def _split_doc(self, v: HtmlElement) -> List[HtmlElement]:
        v1 = v.cssselect(".b-simple_episodes__list .b-simple_episode__item")
        return v1

    def _parse_data_id(self, v: HtmlElement) -> int:
        v1 = v.get("data-id", "")
        v2 = int(v1)
        return v2

    def _parse_data_season_id(self, v: HtmlElement) -> int:
        v1 = v.get("data-season_id", "")
        v2 = int(v1)
        return v2

    def _parse_data_episode_id(self, v: HtmlElement) -> int:
        v1 = v.get("data-episode_id", "")
        v2 = int(v1)
        return v2

    def _parse_title(self, v: HtmlElement) -> str:
        v1 = v.text_content()
        v2 = v1.strip()
        return v2

    def parse(self) -> List[EpisodeType]:
        return [
            {
                "data_id": self._parse_data_id(i),
                "data_season_id": self._parse_data_season_id(i),
                "data_episode_id": self._parse_data_episode_id(i),
                "title": self._parse_title(i),
            }
            for i in self._split_doc(self._doc)
        ]


class PageAnime:
    """
    USAGE
        GET https://hdrezka-home.tv/animation/<NAME>.html

    EXAMPLE
        GET https://hdrezka-home.tv/animation/drama/20157-eksperimenty-leyn-1998.html

    NOTE:
        1. videos required post extract manually

        videos item format (comma separated):
        "[{int}p (Ultra)?]https://...manifest.m3u8 or https://...mp4,[...."

        2. for extract episodes, use next endpoint:

        POST /ajax/get_cdn_series/?t=1775300155352 HTTP/1.1
        Host: hdrezka-home.tv
        X-Requested-With: XMLHttpRequest
        Origin: https://hdrezka-home.tv
        Connection: keep-alive
        Referer: https://hdrezka-home.tv/animation/fiction/88328-daybaster-dotyanis-do-neba-2-2004.html

        id=88328&translator_id=56&season=1&episode=2&favs=aaaaaaaa-bbbb-cccc-dddd-0123456789ab&action=get_stream

        > id, translator_id, season, episode search in 'episode_list', 'season_box', 'translation-list' structs, favs from favs field
    """

    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)
        else:
            self._doc = document
        self._init_script = self._init_init_script(self._doc)

    def _init_init_script(self, v: HtmlElement) -> str:
        v1 = v.cssselect("script")
        v2 = [
            i
            for i in v1
            if any(v in i.text_content() for v in ("sof.tv.initCDNSeriesEvents", "sof.tv.initCDNMoviesEvents"))
        ]
        v3 = v2[0]  # type: ignore[index]
        v4 = v3.text_content()
        return v4

    @classmethod
    def fetch(cls, client: httpx.Client, *, slug: str, **kwargs: Any) -> "PageAnime":
        _kw: Dict[str, Any] = {}
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        _resp = client.request(
            "GET",
            f"https://hdrezka-home.tv/animation/{slug}.html",
            **_kw,
        )
        _resp.raise_for_status()
        _body = _resp.text
        return cls(_body)

    @classmethod
    async def async_fetch(cls, client: httpx.AsyncClient, *, slug: str, **kwargs: Any) -> "PageAnime":
        _kw: Dict[str, Any] = {}
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        _resp = await client.request(
            "GET",
            f"https://hdrezka-home.tv/animation/{slug}.html",
            **_kw,
        )
        _resp.raise_for_status()
        _body = _resp.text
        return cls(_body)

    @classmethod
    def fetch_from_url(cls, client: httpx.Client, *, anime_url: str, **kwargs: Any) -> "PageAnime":
        _kw: Dict[str, Any] = {}
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        _resp = client.request(
            "GET",
            anime_url,
            **_kw,
        )
        _resp.raise_for_status()
        _body = _resp.text
        return cls(_body)

    @classmethod
    async def async_fetch_from_url(cls, client: httpx.AsyncClient, *, anime_url: str, **kwargs: Any) -> "PageAnime":
        _kw: Dict[str, Any] = {}
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        _resp = await client.request(
            "GET",
            anime_url,
            **_kw,
        )
        _resp.raise_for_status()
        _body = _resp.text
        return cls(_body)

    def _parse_title(self, v: HtmlElement) -> str:
        v1 = v.cssselect(".b-post__title h1")[0]
        v2 = v1.text_content()
        return v2

    def _parse_description(self, v: HtmlElement) -> str:
        v1 = v.cssselect(".b-post__description_text")[0]
        v2 = v1.text_content()
        return v2

    def _parse_thumbnail(self, v: HtmlElement) -> str:
        v1 = v.cssselect("img[data-caption-title][src]")[0]
        v2 = v1.get("src", "")
        return v2

    def _parse_favs(self, v: HtmlElement) -> str:
        v1 = v.cssselect("input#ctrl_favs[value]")[0]
        v2 = v1.get("value", "")
        return v2

    def _parse_translation_list(self, v: HtmlElement) -> List[TranslationType]:
        v1 = Translation(v).parse()
        return v1

    def _parse_season_box(self, v: HtmlElement) -> List[SeasonBoxType]:
        v1 = SeasonBox(v).parse()
        return v1

    def _parse_episode_list(self, v: HtmlElement) -> List[EpisodeType]:
        v1 = Episode(v).parse()
        return v1

    def _parse_id(self, v: HtmlElement) -> int:
        try:
            v1 = self._init_script
            v2 = std_re_search(
                "initCDN(?:Series|Movies)Events\\(\\s*(\\d+)",
                v1,
                "hdrezka_parser.kdl:174:24 re-match failed at PageAnime.id pattern=initCDN(?:Series|Movies)Events\\(\\s*(\\d+)",
            )
            v3 = int(v2)
            return v3
        except Exception:
            return 0

    def _parse_translation_id(self, v: HtmlElement) -> str:
        try:
            v1 = self._init_script
            v2 = std_re_search(
                "initCDN(?:Series|Movies)Events\\(\\s*\\d+\\s*,\\s*(\\d+)",
                v1,
                "hdrezka_parser.kdl:179:9 re-match failed at PageAnime.translation_id pattern=initCDN(?:Series|Movies)Events\\(\\s*\\d+\\s*,\\s*(\\d+)",
            )
            return v2
        except Exception:
            return ""

    def parse(self) -> PageAnimeType:
        return {
            "title": self._parse_title(self._doc),
            "description": self._parse_description(self._doc),
            "thumbnail": self._parse_thumbnail(self._doc),
            "favs": self._parse_favs(self._doc),
            "translation_list": self._parse_translation_list(self._doc),
            "season_box": self._parse_season_box(self._doc),
            "episode_list": self._parse_episode_list(self._doc),
            "id": self._parse_id(self._doc),
            "translation_id": self._parse_translation_id(self._doc),
        }


@dataclass(frozen=True)
class HdrezkaCdnSeriesAPIErr200Success(Err[HdrezkaCdnErrorResponseJson]):
    status: Literal[200] = 200


GetStreamResult = Union[Ok[HdrezkaCdnResponseJson], HdrezkaCdnSeriesAPIErr200Success, UnknownErr, TransportErr]

GetMovieResult = Union[Ok[HdrezkaCdnResponseJson], HdrezkaCdnSeriesAPIErr200Success, UnknownErr, TransportErr]

HDREZKA_CDN_SERIES_API_MATCHERS: List[ErrMatcher] = [
    ErrMatcher(200, lambda _b: _b.get("success") is False, HdrezkaCdnSeriesAPIErr200Success),
]


class HdrezkaCdnSeriesAPI:
    @classmethod
    def get_stream(
        cls,
        client: httpx.Client,
        *,
        timestamp: int,
        id: str,
        translator_id: str,
        season: str,
        episode: str,
        favs: str,
        **kwargs: Any,
    ) -> GetStreamResult:
        _kw: Dict[str, Any] = {}
        _kw["headers"] = {
            "Accept": "application/json, text/javascript, */*; q=0.01",
            "Content-Type": "application/x-www-form-urlencoded; charset=UTF-8",
            "X-Requested-With": "XMLHttpRequest",
            "Origin": "https://hdrezka-home.tv",
        }
        _kw["params"] = {"t": timestamp}
        _kw["data"] = {
            "id": id,
            "translator_id": translator_id,
            "season": season,
            "episode": episode,
            "favs": favs,
            "action": "get_stream",
        }
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        return cast(
            GetStreamResult,
            ssc_rest_call(
                client,
                HDREZKA_CDN_SERIES_API_MATCHERS,
                "POST",
                "https://hdrezka-home.tv/ajax/get_cdn_series/",
                **_kw,
            ),
        )

    @classmethod
    async def async_get_stream(
        cls,
        client: httpx.AsyncClient,
        *,
        timestamp: int,
        id: str,
        translator_id: str,
        season: str,
        episode: str,
        favs: str,
        **kwargs: Any,
    ) -> GetStreamResult:
        _kw: Dict[str, Any] = {}
        _kw["headers"] = {
            "Accept": "application/json, text/javascript, */*; q=0.01",
            "Content-Type": "application/x-www-form-urlencoded; charset=UTF-8",
            "X-Requested-With": "XMLHttpRequest",
            "Origin": "https://hdrezka-home.tv",
        }
        _kw["params"] = {"t": timestamp}
        _kw["data"] = {
            "id": id,
            "translator_id": translator_id,
            "season": season,
            "episode": episode,
            "favs": favs,
            "action": "get_stream",
        }
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        return cast(
            GetStreamResult,
            await ssc_rest_call_async(
                client,
                HDREZKA_CDN_SERIES_API_MATCHERS,
                "POST",
                "https://hdrezka-home.tv/ajax/get_cdn_series/",
                **_kw,
            ),
        )

    @classmethod
    def get_movie(
        cls, client: httpx.Client, *, timestamp: int, id: str, translator_id: str, favs: str, **kwargs: Any
    ) -> GetMovieResult:
        _kw: Dict[str, Any] = {}
        _kw["headers"] = {
            "Accept": "application/json, text/javascript, */*; q=0.01",
            "Content-Type": "application/x-www-form-urlencoded; charset=UTF-8",
            "X-Requested-With": "XMLHttpRequest",
            "Origin": "https://hdrezka-home.tv",
        }
        _kw["params"] = {"t": timestamp}
        _kw["data"] = {"id": id, "translator_id": translator_id, "favs": favs, "action": "get_movie"}
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        return cast(
            GetMovieResult,
            ssc_rest_call(
                client,
                HDREZKA_CDN_SERIES_API_MATCHERS,
                "POST",
                "https://hdrezka-home.tv/ajax/get_cdn_series/",
                **_kw,
            ),
        )

    @classmethod
    async def async_get_movie(
        cls, client: httpx.AsyncClient, *, timestamp: int, id: str, translator_id: str, favs: str, **kwargs: Any
    ) -> GetMovieResult:
        _kw: Dict[str, Any] = {}
        _kw["headers"] = {
            "Accept": "application/json, text/javascript, */*; q=0.01",
            "Content-Type": "application/x-www-form-urlencoded; charset=UTF-8",
            "X-Requested-With": "XMLHttpRequest",
            "Origin": "https://hdrezka-home.tv",
        }
        _kw["params"] = {"t": timestamp}
        _kw["data"] = {"id": id, "translator_id": translator_id, "favs": favs, "action": "get_movie"}
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        return cast(
            GetMovieResult,
            await ssc_rest_call_async(
                client,
                HDREZKA_CDN_SERIES_API_MATCHERS,
                "POST",
                "https://hdrezka-home.tv/ajax/get_cdn_series/",
                **_kw,
            ),
        )
