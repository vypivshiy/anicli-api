# autogenerated by ssc-gen. DO NOT EDIT
from lxml import html
from lxml.html import HtmlElement
from typing import Any, Dict, List, TypedDict, Union
from typing import cast
import httpx

from .sscgen_runtime import (
    FALLBACK_HTML_STR,
    Ok,
    UnknownErr,
    TransportErr,
    ErrMatcher,
    ssc_rest_call,
    ssc_rest_call_async,
)

from .sscgen_runtime import std_re_search

PlaylistJson = TypedDict(
    "PlaylistJson",
    {
        "title": str,
        "file": str,
        "thumbnails": str,
    },
)
PageOngoingType = TypedDict(
    "PageOngoingType",
    {
        "url": str,
        "title": str,
        "thumbnail": str,
    },
)
PageSearchType = TypedDict(
    "PageSearchType",
    {
        "title": str,
        "thumbnail": str,
        "url": str,
    },
)
PageAnimeType = TypedDict(
    "PageAnimeType",
    {
        "title": str,
        "description": str,
        "thumbnail": str,
        "player_url": str,
    },
)
PagePlaylistURLType = TypedDict(
    "PagePlaylistURLType",
    {
        "playlist_url": str,
    },
)


class PageOngoing:
    "USAGE: \nGET https://sameband.studio/novinki"

    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)
        else:
            self._doc = document

    def _split_doc(self, v: HtmlElement) -> List[HtmlElement]:
        v1 = v.cssselect(".col-auto")
        return v1

    @classmethod
    def fetch(cls, client: httpx.Client, **kwargs: Any) -> "PageOngoing":
        _kw: Dict[str, Any] = {}
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        _resp = client.request(
            "GET",
            "https://sameband.studio/novinki",
            **_kw,
        )
        _resp.raise_for_status()
        _body = _resp.text
        return cls(_body)

    @classmethod
    async def async_fetch(cls, client: httpx.AsyncClient, **kwargs: Any) -> "PageOngoing":
        _kw: Dict[str, Any] = {}
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        _resp = await client.request(
            "GET",
            "https://sameband.studio/novinki",
            **_kw,
        )
        _resp.raise_for_status()
        _body = _resp.text
        return cls(_body)

    def _parse_url(self, v: HtmlElement) -> str:
        v1 = v.cssselect("a[href]")[0]
        v2 = v1.get("href", "")
        return v2

    def _parse_title(self, v: HtmlElement) -> str:
        v1 = v.cssselect(".col-auto .poster[title]")[0]
        v2 = v1.get("title", "")
        return v2

    def _parse_thumbnail(self, v: HtmlElement) -> str:
        v1 = v.cssselect("img.swiper-lazy[src]")[0]
        v2 = v1.get("src", "")
        v3 = "https://sameband.studio{}".format(v2)
        return v3

    def parse(self) -> List[PageOngoingType]:
        return [
            {
                "url": self._parse_url(i),
                "title": self._parse_title(i),
                "thumbnail": self._parse_thumbnail(i),
            }
            for i in self._split_doc(self._doc)
        ]


class PageSearch:
    "USAGE:\n    POST https://sameband.studio/index.php?do=search\n    do=search&subaction=search&search_start=0&full_search=0&result_from=1&story=<QUERY>\n\nNOTE:\n    search query len should be 4 or more characters. And in manual tests, works only cyrillic queries\n\nEXAMPLE:\n\n    POST https://sameband.studio/index.php?do=search\n    do=search&subaction=search&search_start=0&full_search=0&result_from=1&story=ВЕДЬ"

    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)
        else:
            self._doc = document

    @classmethod
    def fetch(cls, client: httpx.Client, *, query: str, **kwargs: Any) -> "PageSearch":
        _kw: Dict[str, Any] = {}
        _kw["headers"] = {"Content-Type": "application/x-www-form-urlencoded"}
        _kw["params"] = {"do": "search"}
        _kw["data"] = {
            "do": "search",
            "subaction": "search",
            "search_start": "0",
            "full_search": "0",
            "result_from": "1",
            "story": query,
        }
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        _resp = client.request(
            "POST",
            "https://sameband.studio/index.php",
            **_kw,
        )
        _resp.raise_for_status()
        _body = _resp.text
        return cls(_body)

    @classmethod
    async def async_fetch(cls, client: httpx.AsyncClient, *, query: str, **kwargs: Any) -> "PageSearch":
        _kw: Dict[str, Any] = {}
        _kw["headers"] = {"Content-Type": "application/x-www-form-urlencoded"}
        _kw["params"] = {"do": "search"}
        _kw["data"] = {
            "do": "search",
            "subaction": "search",
            "search_start": "0",
            "full_search": "0",
            "result_from": "1",
            "story": query,
        }
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        _resp = await client.request(
            "POST",
            "https://sameband.studio/index.php",
            **_kw,
        )
        _resp.raise_for_status()
        _body = _resp.text
        return cls(_body)

    def _split_doc(self, v: HtmlElement) -> List[HtmlElement]:
        v1 = v.cssselect(".col-auto")
        return v1

    def _parse_title(self, v: HtmlElement) -> str:
        v1 = v.cssselect(".col-auto .poster[title]")[0]
        v2 = v1.get("title", "")
        return v2

    def _parse_thumbnail(self, v: HtmlElement) -> str:
        v1 = v.cssselect("img.swiper-lazy[src]")[0]
        v2 = v1.get("src", "")
        v3 = "https://sameband.studio{}".format(v2)
        return v3

    def _parse_url(self, v: HtmlElement) -> str:
        v1 = v.cssselect(".image[href]")[0]
        v2 = v1.get("href", "")
        return v2

    def parse(self) -> List[PageSearchType]:
        return [
            {
                "title": self._parse_title(i),
                "thumbnail": self._parse_thumbnail(i),
                "url": self._parse_url(i),
            }
            for i in self._split_doc(self._doc)
        ]


class PageAnime:
    "USAGE:\n\n    GET https://sameband.studio/anime/<ANIME PATH>.html\n\nEXAMPLE:\n    curl https://sameband.studio/anime/20-госпожа-кагуя-3.html\n    curl https://sameband.studio/anime/20-%D0%B3%D0%BE%D1%81%D0%BF%D0%BE%D0%B6%D0%B0-%D0%BA%D0%B0%D0%B3%D1%83%D1%8F-3.html"

    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)
        else:
            self._doc = document

    def _parse_title(self, v: HtmlElement) -> str:
        v1 = v.cssselect("h1.p-0.m-0")[0]
        v2 = v1.text_content()
        return v2

    def _parse_description(self, v: HtmlElement) -> str:
        v1 = v.cssselect(".limiter p")
        v2 = [i.text_content() for i in v1]
        v3 = " ".join(v2)
        return v3

    def _parse_thumbnail(self, v: HtmlElement) -> str:
        v1 = v.cssselect(".image > img[src]")[0]
        v2 = v1.get("src", "")
        v3 = "https://sameband.studio{}".format(v2)
        return v3

    @classmethod
    def fetch(cls, client: httpx.Client, *, anime_page_url: str, **kwargs: Any) -> "PageAnime":
        _kw: Dict[str, Any] = {}
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        _resp = client.request(
            "GET",
            anime_page_url,
            **_kw,
        )
        _resp.raise_for_status()
        _body = _resp.text
        return cls(_body)

    @classmethod
    async def async_fetch(cls, client: httpx.AsyncClient, *, anime_page_url: str, **kwargs: Any) -> "PageAnime":
        _kw: Dict[str, Any] = {}
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        _resp = await client.request(
            "GET",
            anime_page_url,
            **_kw,
        )
        _resp.raise_for_status()
        _body = _resp.text
        return cls(_body)

    def _parse_player_url(self, v: HtmlElement) -> str:
        v1 = v.cssselect(".player > .player-content > iframe[src]")[0]
        v2 = v1.get("src", "")
        v3 = "https://sameband.studio{}".format(v2)
        return v3

    def parse(self) -> PageAnimeType:
        return {
            "title": self._parse_title(self._doc),
            "description": self._parse_description(self._doc),
            "thumbnail": self._parse_thumbnail(self._doc),
            "player_url": self._parse_player_url(self._doc),
        }


PlaylistResult = Union[Ok[List[PlaylistJson]], UnknownErr, TransportErr]

PLAYLIST_TXT_API_MATCHERS: List[ErrMatcher] = []


class PlaylistTxtAPI:
    @classmethod
    def playlist(cls, client: httpx.Client, *, playlist_txt_url: str, **kwargs: Any) -> PlaylistResult:
        _kw: Dict[str, Any] = {}
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        return cast(
            PlaylistResult,
            ssc_rest_call(
                client,
                PLAYLIST_TXT_API_MATCHERS,
                "GET",
                playlist_txt_url,
                **_kw,
            ),
        )

    @classmethod
    async def async_playlist(cls, client: httpx.AsyncClient, *, playlist_txt_url: str, **kwargs: Any) -> PlaylistResult:
        _kw: Dict[str, Any] = {}
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        return cast(
            PlaylistResult,
            await ssc_rest_call_async(
                client,
                PLAYLIST_TXT_API_MATCHERS,
                "GET",
                playlist_txt_url,
                **_kw,
            ),
        )


class PagePlaylistURL:
    'USAGE:\n\n    GET https://sameband.studio/pl/a/<PLAYLIST NAME>.html\n\nEXAMPLE:\n\n    GET https://sameband.studio/pl/a/Mashle_2nd_Season.html\n\nNOTE:\n\n    url contains in AnimeView.player_url key:\n\n    playlist items signature (need manually provide json unmarshall logic):\n\n    ```\n        [\n            {\n                "title": "<img src=\'/v/anime/...01 RUS_snapshot.jpg\' class=playlist_poster><div class=playlist_duration>23:37</div>... 01",\n                ### delimiter - \',\'\n                "file": "[480p]/v/anime/... - 01 RUS_480p/... - 01 RUS_r480p.m3u8,[720p]/v/anime/.../... - 01 RUS_720p/... - 01 RUS_r720p.m3u8,[1080p]/v/anime/.../... -\n                01 RUS_1080p/... - 01 RUS_r1080p.m3u8",\n                ### thumbnails images for video\n                "thumbnails": "/v/anime/.../thumbnails/... - 01 RUS.txt"  # contains\n            },\n            {\n                ...\n            },\n            ...\n        ]\n    ```\n    player script signature:\n    ```\n    <script>var player = new Playerjs({id:"player",file:"/v/list/....txt"});\n    ```'

    def __init__(self, document: Union[str, HtmlElement]):
        if isinstance(document, str):
            self._doc = html.fromstring(document.strip() or FALLBACK_HTML_STR)
        else:
            self._doc = document

    @classmethod
    def fetch(cls, client: httpx.Client, *, player_url: str, **kwargs: Any) -> "PagePlaylistURL":
        _kw: Dict[str, Any] = {}
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        _resp = client.request(
            "GET",
            player_url,
            **_kw,
        )
        _resp.raise_for_status()
        _body = _resp.text
        return cls(_body)

    @classmethod
    async def async_fetch(cls, client: httpx.AsyncClient, *, player_url: str, **kwargs: Any) -> "PagePlaylistURL":
        _kw: Dict[str, Any] = {}
        for _k, _v in kwargs.items():
            if isinstance(_kw.get(_k), dict) and isinstance(_v, dict):
                _kw[_k] = {**_kw[_k], **_v}
            else:
                _kw[_k] = _v
        _resp = await client.request(
            "GET",
            player_url,
            **_kw,
        )
        _resp.raise_for_status()
        _body = _resp.text
        return cls(_body)

    def _parse_playlist_url(self, v: HtmlElement) -> str:
        v1 = html.tostring(v, encoding="unicode")
        v2 = std_re_search(
            "Playerjs[^>]+file:\\s*[\\\"']([^>]+)[\\\"']",
            v1,
            "sameband_parser.kdl:149:9 re-match failed at PagePlaylistURL.playlist_url pattern=Playerjs[^>]+file:\\s*[\\\"']([^>]+)[\\\"']",
        )
        v3 = "https://sameband.studio{}".format(v2)
        return v3

    def parse(self) -> PagePlaylistURLType:
        return {
            "playlist_url": self._parse_playlist_url(self._doc),
        }
